---
id: deployment
title: Deployment
sidebar_label: Deployment
keywords: ["webiny", "stacks", "deployment", "pulumi", "iac"]
description: Deployment
---

import Theme from "../../mdxComponents/theme";
import { Box, Button, Grid, Link } from "theme-ui";

🧶

---

## Key files
maybe can bee used :down:
Rephrasing this, from a Webiny project application's perspective, every application is supported by a Pulumi program. A Pulumi program can consist of one or more Pulumi program stacks, which essentially enable you to deploy multiple sets of cloud infrastructure resources. These sets can be exact copies, but can also be different. Respectively, this makes it possible to form a notion of multiple environments, or deploy different infrastructure if a new feature demands it.

When talking about stack deployments, by default, Webiny relies on [Pulumi](https://www.pulumi.com/), which is a modern [infrastructure as code](https://docs.aws.amazon.com/whitepapers/latest/introduction-devops-aws/infrastructure-as-code.html) solution.

Every Webiny project stack has a couple of Pulumi-related files. In the following folder structure, we are looking at three stacks that are set up in every new Webiny project by default:

```
.
├── api
│   ├── Pulumi.yaml
│   ├── Pulumi.dev.yaml
│   ├── index.ts
│   └── stack
├── apps
│   ├── admin
│   │   ├── Pulumi.yaml
│   │   ├── Pulumi.dev.yaml
│   │   ├── index.ts
│   │   └── stack
│   ├── site
│   │   ├── Pulumi.dev.yaml
│   │   ├── Pulumi.yaml
│   │   ├── index.ts
│   │   └── stack
│   └── theme
└── (...)
```

