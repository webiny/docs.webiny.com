AI Context: 6.4.10 Changelog (changelog.mdx)

This file tracks manual edits made after the generation script ran.
The script reads the "Skipped PRs" section to avoid re-adding removed entries.

## Skipped PRs

## Manual Rewrites

- #5631 — entry written by hand. The generator discovers PRs via the GitHub milestone and
  this PR was missing from the generated output, so the "Infra.Vpc enabled={false} Is Now
  Respected in Production" entry under "## Infrastructure" was authored manually from the PR
  body. The destructive-change warning (VPC teardown, core-before-api deploy order) comes from
  the "One caveat for users" section of the PR description.
