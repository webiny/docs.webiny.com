AI Context: 6.4.4 Changelog (changelog.mdx)

This file tracks manual edits made after the generation script ran.
The script reads the "Skipped PRs" section to avoid re-adding removed entries.

## Skipped PRs

## Manual Rewrites

- #5373: Manually written entry for security fix (prototype pollution in `transformWhereToNested`). PR description was minimal ("Security issue: second-order prototype pollution vulnerability"), so the changelog entry was written from the diff to explain the attack vector and the two-part fix.
