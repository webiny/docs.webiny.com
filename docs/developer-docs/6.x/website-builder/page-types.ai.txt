AI Context: Page Types (page-types.mdx)

Source of Information:
1. /Users/adrian/dev/wby-next2/extensions/customPageTypes/RetailPageType.ts — working implementation of PageType.Interface
2. /Users/adrian/dev/wby-next2/extensions/customPageTypes/FilterPageTypes.ts — working implementation of PageTypeProvider decorator
3. /Users/adrian/dev/wby-next2/extensions/customPageTypes/index.tsx — feature registration using createFeature + RegisterFeature
4. /Users/adrian/dev/wby-next2/webiny.config.tsx — Admin.Extension registration pattern

Key Documentation Decisions:
1. API has changed from JSX-based PageListConfig.PageType to the DI pattern (PageType.Interface + PageTypeProvider.Interface) — old docs were based on funnelBuilder example, now replaced entirely
2. No custom form component per page type in the new API — a page type is just name + label
3. Filter section uses decorator pattern — class receives decoratee, filters getPageTypes() result
4. Feature registration (createFeature + RegisterFeature) is the wiring mechanism — consistent with other admin extensibility docs
5. Split into three files matching the actual source structure (RetailPageType.ts, FilterPageTypes.ts, index.tsx)

Understanding:
PageType.Interface requires: name (string, stored on the page at creation) and label (string, shown in the dropdown).
PageTypeProvider.Interface requires: getPageTypes() method. The decorator receives the decoratee provider in its constructor, delegates to it, and filters/transforms the result.
Registration via container.register() for implementations and container.registerDecorator() for decorators — same pattern as other DI-based extensions.
The FilterPageTypes.ts in the source has the filter commented out — the actual filter logic is .filter(type => type.name !== "static").

Related Documents:
- docs/developer-docs/6.x/website-builder/customize-page-list-columns.mdx — other WB list extensibility
- docs/developer-docs/6.x/website-builder/custom-component.mdx — other WB extensibility
- docs/developer-docs/6.x/core-concepts/di.mdx — DI pattern foundational doc

Key Code Locations:
- /Users/adrian/dev/wby-next2/extensions/customPageTypes/ — working reference implementation

Tone Guidelines:
- Practical and direct — the API is simple, keep explanations brief
- Show all three files (RetailPageType.ts, FilterPageTypes.ts, index.tsx) as separate code blocks with file paths
- Explain the decorator pattern briefly in the filter section — not all readers will know what decoratee means
