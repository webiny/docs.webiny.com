AI Context: webiny configure-mcp (configure-mcp.mdx)

## Source of Information

1. User-provided CLI help output for `webiny configure-mcp`

## Key Documentation Decisions

1. Follows the same structure as other CLI articles (deploy.mdx, watch.mdx)
2. Default agent is `claude` — documented in both prose and options table
3. `--instructions` flag documented — prints setup instructions rather than writing config
4. Positional `agent` argument treated like a positional in the usage examples

## Understanding

### Supported agents

claude, cursor, windsurf, copilot, cline

### What the command does

Writes MCP server configuration for the chosen agent so it can communicate with the Webiny project via the Model Context Protocol.

## Related Documents

- `cli/deploy.mdx` — reference for article style/structure

## Tone Guidelines

- Same concise reference style as other CLI articles
- No explanation of what MCP is — link to relevant article if needed in future
