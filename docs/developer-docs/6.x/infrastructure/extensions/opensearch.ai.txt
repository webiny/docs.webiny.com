AI Context: Amazon OpenSearch (infrastructure/extensions/opensearch.mdx)

Source of Information:
1. v5.40.x reference: docs/developer-docs/5.40.x/infrastructure/basics/modify-cloud-infrastructure.mdx (Amazon OpenSearch sections)
2. reference/extensions.mdx — Infra.OpenSearch props
3. User-provided webiny.config.tsx showing real v6 usage

Key Documentation Decisions:
- Dropped ElasticSearch section entirely — v6 only supports OpenSearch
- Adjusting config section uses CorePulumi extension with onResource pattern (mirrors v5 onResource callback)
- Shared domain example uses Infra.Env.Is to scope it to dev only, matching real-world usage
- indexPrefix explanation kept — non-obvious but important to prevent data bleed between envs
- Warning about shared domain only for dev is retained from v5

Related Documents:
- infrastructure/extensions/env-specific-config.mdx — Infra.Env.Is pattern used in shared domain example
- infrastructure/extensions/pulumi-extensions.mdx — CorePulumi pattern
- reference/extensions.mdx — full Infra.OpenSearch props

Tone Guidelines:
- Practical, production-ready
- Keep the dev-only warning visible
