---
id: 9ac642e0
title: Configure MCP Server
description: Learn how to set up the Webiny MCP server so your AI coding agent can access Webiny skills and documentation.
---

import { Alert } from "@/components/Alert";

<Alert type="success" title="WHAT YOU'LL LEARN">

- what the Webiny MCP server is and what it provides
- how to configure it for your AI coding agent
- how to add or override skills folders

</Alert>

## Overview

The Webiny MCP server gives your AI coding agent access to Webiny-specific knowledge through the [Model Context Protocol (MCP)](https://modelcontextprotocol.io/). Instead of relying on the agent's general training data, the MCP server provides up-to-date, structured skills — detailed guides on Webiny APIs, patterns, and project conventions — directly to the agent when it needs them.

The server exposes two tools:

- **`list_webiny_skills`** — returns a catalog of all available skills with names and descriptions
- **`get_webiny_skill`** — loads the full documentation for a specific skill by name

Your agent calls these tools automatically when working on Webiny-related tasks, giving it the context it needs to write correct code.

## Quick Setup

The fastest way to configure the MCP server is to run the built-in setup command. It writes the necessary config files for your agent automatically.

```
npx webiny configure-mcp claude
```

Replace `claude` with your agent: `cursor`, `windsurf`, `copilot`, or `cline`.

This command creates two files:

- **MCP config file** — tells your agent how to start the Webiny MCP server
- **Instruction file** — tells your agent to use the Webiny tools when working on Webiny tasks

Once done, restart your agent or reload the window, and the Webiny MCP server becomes available.

## Manual Setup

If your agent is not covered by the built-in adapters, or you prefer to configure things yourself, follow the steps below.

### Register the MCP Server

Add the following entry to your agent's MCP configuration file:

```json
{
  "mcpServers": {
    "webiny": {
      "command": "npx",
      "args": ["webiny", "mcp-server"]
    }
  }
}
```

The location of this file depends on your agent:

| Agent                    | Config file                                                               |
| ------------------------ | ------------------------------------------------------------------------- |
| Claude Code              | `.mcp.json` (project root)                                                |
| Cursor                   | `.cursor/mcp.json` (project root)                                         |
| Windsurf                 | `.windsurf/mcp.json` (project root)                                       |
| GitHub Copilot / VS Code | `.vscode/mcp.json` (uses `servers` key, not `mcpServers`)                 |
| Cline                    | `.vscode/cline_mcp_settings.json`                                         |
| Zed                      | `~/.config/zed/settings.json` (under `context_servers`)                   |
| Claude Desktop           | `~/Library/Application Support/Claude/claude_desktop_config.json` (macOS) |

<Alert type="info" title="GitHub Copilot uses a different key">

GitHub Copilot expects `servers` instead of `mcpServers` in its config file. The rest of the structure is the same.

</Alert>

### Add Agent Instructions

Add the following to your agent's instruction or rules file so it knows to use the Webiny tools:

```
## Webiny

This project uses the Webiny framework.
A `webiny` MCP server is available.
When helping with Webiny-related tasks:
1. Call `list_webiny_skills` to see available skills.
2. Call `get_webiny_skill` with the relevant topic before writing code.
```

The location of the instruction file depends on your agent:

| Agent                    | Instruction file                  |
| ------------------------ | --------------------------------- |
| Claude Code              | `CLAUDE.md` (project root)        |
| Cursor                   | `.cursor/rules/*.mdc`             |
| Windsurf                 | `.windsurf/rules/*.md`            |
| GitHub Copilot / VS Code | `.github/copilot-instructions.md` |
| Cline                    | System prompt in settings         |
| Zed                      | System prompt in settings         |

### Verify the Server

Test that the server starts correctly using the MCP Inspector:

```
npx @modelcontextprotocol/inspector npx webiny mcp-server
```

Connect, click **List Tools**, and confirm you see `list_webiny_skills` and `get_webiny_skill`.

## Custom Skills Folders

The MCP server ships with a set of built-in skills that cover standard Webiny development patterns. You can extend or replace these skills using CLI flags.

### Adding Extra Skills

Use the `--additional-skills` flag to add a folder of custom skills on top of the built-in ones:

```json
{
  "mcpServers": {
    "webiny": {
      "command": "npx",
      "args": ["webiny", "mcp-server", "--additional-skills=./my-skills"]
    }
  }
}
```

You can repeat this flag to add multiple folders:

```json
{
  "mcpServers": {
    "webiny": {
      "command": "npx",
      "args": [
        "webiny",
        "mcp-server",
        "--additional-skills=./team-skills",
        "--additional-skills=./project-skills"
      ]
    }
  }
}
```

When a skill with the same name exists in multiple folders, the one in the highest-priority folder wins. Priority is determined by order: folders listed later on the command line take precedence over earlier ones, and all additional folders take precedence over the built-in skills.

### Replacing Built-in Skills

Use the `--skills` flag to replace the built-in skills folder entirely:

```json
{
  "mcpServers": {
    "webiny": {
      "command": "npx",
      "args": ["webiny", "mcp-server", "--skills=./my-skills"]
    }
  }
}
```

When you use `--skills`, only skills found in the specified folder are served. The built-in skills are ignored completely.

### Writing a Skill

Each skill is a folder containing a single `SKILL.md` file. The file must start with YAML front-matter:

```
---
name: my-custom-skill
description: >
  A short description of what this skill covers.
  The agent uses this to decide when to load the skill.
context: webiny-extensions
---

# My Custom Skill

The rest of the file is the skill content — markdown documentation
that gets returned to the agent when it calls `get_webiny_skill`.
```

**Front-matter fields:**

- **`name`** (required) — unique identifier for the skill, used as the `topic` argument in `get_webiny_skill`
- **`description`** (required) — shown in the skill catalog; helps the agent decide which skill to load
- **`context`** (optional) — groups skills in the catalog; defaults to `webiny-extensions`

Place the `SKILL.md` file inside a descriptively named folder:

```
my-skills/
├── custom-auth-flow/
│   └── SKILL.md
└── data-migration/
    └── SKILL.md
```

Paths passed to `--skills` and `--additional-skills` are resolved relative to the current working directory (your project root).
